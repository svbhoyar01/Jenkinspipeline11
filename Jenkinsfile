pipeline {
    agent any

    stages {
        stage('Checkout') {
             steps{
                echo 'Git'
            }
        }
        stage('Build') {
             steps{
                echo 'build'
            }
        }
        
        stage('test') {
             steps{
                echo 'test'
            }
        }
        stage('depoly') {
             steps{
                echo 'deploy'
            }
        }
    }
}
